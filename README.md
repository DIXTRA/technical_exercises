# Technical interview by DIXTRA

### Getting started

After cloning this repo follow the next commands to run the project successfully:

```bash
npm install # install deps
npm run dev # start the dev server
```